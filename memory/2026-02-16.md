# 2026-02-16

## OpenClaw Update: v2026.2.14 → v2026.2.15
- **Security hardening:** SHA-256 sandbox hashing, token redaction in logs, sandbox Docker escape prevention, path sanitization, XSS fixes
- **Discord Components v2:** Interactive buttons, select menus, modals, file blocks (native interaction)
  - Format: `message` tool with `components` payload
  - Supported blocks: `text`, `section`, `separator`, `actions`, `media-gallery`, `file`
  - Button styles: `primary`, `secondary`, `success`, `danger`, `link`
  - Modal forms: up to 5 fields (text/checkbox/radio/select)
  - **No config flag required** — feature enabled by default in v2026.2.15
  - Created reference docs: `discord-components-v2.md` + updated `TOOLS.md`
- **Nested subagents:** Sub-agents can spawn their own children (configurable depth, max 5 children per agent)
- **Multi-channel improvements:** Per-channel ack reaction overrides, cron webhook delivery toggle, Telegram streaming fixes
- **Critical fixes:** 
  - Group chat context now injected on every turn (was losing awareness mid-conversation)
  - Timezone-aware memory dates
  - Discord role allowlist matching
  - TUI copy-paste token preservation
  - Multi-turn OpenAI `store=true` for Codex/Responses
  - FTS Unicode awareness for CJK queries

## File Delivery Protocol (New)
- **Rule added to AGENTS.md:** When creating files for Ayoub:
  1. Save to workspace (Mac copy)
  2. Send via Discord using `message` tool with `filePath` (instant download)
  - Both locations required every time

## Components v2 Testing
- Initial test failed (showed as plain text)
- Investigated:
  - Ran `openclaw doctor --fix` (cleaned up deprecated config)
  - Checked schema: `agentComponents` does NOT exist (was a false lead from docs mention)
  - Components v2 works at message tool level, no config flag needed
- Format used (per docs):
  ```json
  {
    "channel": "discord",
    "action": "send",
    "to": "channel:ID",
    "components": {
      "text": "Prompt text",
      "blocks": [{ "type": "actions", "buttons": [...] }]
    }
  }
  ```
- **Status:** Awaiting Ayoub's confirmation whether buttons rendered

## Learning: exec Error Suppression
- Ayoub reminder: redirect exec debugging errors to `/tmp/vex-debug.log`
- Pattern: `command 2>&1 > /tmp/vex-debug.log` or `&> /tmp/vex-debug.log`
- Only surface actual failures that block the task

## Clawtex Onboarding
- Created Spanish prompt for Clawtex team: `clawtex-discord-components-prompt.txt`
- Delivered both as workspace file + Discord attachment

## Components v2 Status (BROKEN in v2026.2.15)
- **Issue #18161:** Buttons/selects/modals silently dropped by message tool
- **Workaround:** Use legacy embeds + polls until fixed
- **TODO:** Test Components v2 again in v2026.2.16+ (Ayoub requested - 2026-02-16)
- **Poll test result:** Polls work perfectly; Vex misread poll response (voted "Yes", not "Option B") - interpretation bug, not functionality issue

## X Account Suspended (@vex00x00)
- **Status:** Account suspended by X (reported by Ayoub 2026-02-16)
- **Account:** @vex00x00 (vex00x00@gmail.com)
- **Context:** Used for Daily AI Brief newsletter curation (44-account seed list complete 2026-02-11)
- **Impact:** Cannot curate X feed for newsletter content
- **Next steps:** Awaiting Ayoub's decision on appeal/new account/alternative source
