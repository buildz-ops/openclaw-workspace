# 2026-02-21

## AI News & Search Strategy
- **Search Router:** Brave is default for "factual" queries (news/events).
- **X (Twitter) Integration:**
    - Tried using `web_search` with `site:x.com` -> 0 results (indexing lag/blocks).
    - Browser automation on `x.com/explore` and `x.com/search` works for real-time trends.
    - **Current Trend (X):** OpenAI hiring Peter Steinberger (OpenClaw creator) is viral (24K+ posts).
    - **Configuration:** Updated `ai intel` category channels (`ai-bookmarks`, `news-setup`, `ai-newsletter`) to default to **Grok** (`model="grok"`) for better X-native knowledge, via `systemPrompt` override in `openclaw.json`.

## X Account Growth
- **Seed List:** Verified existence of `skills/x-bookmarks/seed-following.json` containing the original 44 accounts (AlexFinn, aiDotEngineer, etc.).
