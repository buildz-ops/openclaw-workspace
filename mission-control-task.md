# PENDING TASKS

## Mission Control Dashboard (Trigger: "Build Mission Control")
- **Goal:** Build a local "Mission Control" dashboard for OpenClaw.
- **Tech Stack:** Next.js + Tailwind CSS.
- **Design:** "Liquid Glass" dark mode (deep blues, translucent cards, glowing accents) + Light mode toggle.
- **Features:** Sidebar nav, Status Cards (Online/Idle, Queue, Load), Live Activity Feed (real OpenClaw logs/heartbeats).
- **Reference:** Based on analyzed TikTok video frames (temp_video/frames).
- **Procedure:**
    1.  **Switch Model:** `github-copilot/gpt-5.2-codex` immediately.
    2.  **Scaffold:** Create `mission-control` project.
    3.  **Execute:** Build full MVP (frontend + local API integration).
    4.  **Target Time:** ~2 hours.

*Context: User wants this done in the afternoon (approx 13:00 start -> 15:00 finish).*
